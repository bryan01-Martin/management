{
    "name": "management",
    "version": "1.0.0",
    "license": "MIT",
    "scripts": {
        "client": "cd client && yarn start",
        "server": "nodemon server.js",
        "dev": "npx concurrently --kill-others-on-fail \"yarn client\" \"cross-env START_TYPE=dev yarn server\""
    },
    "dependencies": {
        "body-parser": "^1.19.0",
        "cross-env": "^7.0.3",
        "dotenv": "^8.2.0",
        "express": "^4.17.1",
        "multer": "^1.4.2",
        "nodemon": "^2.0.7",
        "oracledb": "^5.1.0",
        "react-scripts": "^4.0.3"
    },
    "devDependencies": {
        "concurrently": "^6.0.2"
    }
}
